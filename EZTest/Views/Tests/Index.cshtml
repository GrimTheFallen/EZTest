@model IQueryable<EZTest.Models.Test>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
 
@Html.Partial("~/Views/Grids/TaskGrid.cshtml", Model)

@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.Label("Ogólna ilość pytań")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TestQuestions.Count)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.TestId }) |
            @Html.ActionLink("Details", "Details", new { id = item.TestId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.TestId })
        </td>
    </tr>
}

</table>*@
